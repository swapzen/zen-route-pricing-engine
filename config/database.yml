default: &default
  adapter: cockroachdb
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  port: 26257

development:
  <<: *default
  database: swapzen_development  # SHARED with swapzen-api
  username: root
  password:
  host: localhost

test:
  <<: *default
  database: swapzen_test  # SHARED with swapzen-api
  username: root
  password:
  host: localhost

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>  # SHARED cluster
