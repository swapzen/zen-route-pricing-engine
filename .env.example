# =============================================================================
# ZEN ROUTE PRICING ENGINE - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git - it contains sensitive secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# GOOGLE MAPS API (Required for Production)
# -----------------------------------------------------------------------------
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Route provider strategy: 'google', 'local', or 'haversine'
# - google: Use Google Maps API (production)
# - local/haversine: Use Haversine fallback (development/testing)
ROUTE_PROVIDER_STRATEGY=google

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# CockroachDB connection (shared with swapzen-api)
DATABASE_URL=postgresql://root@localhost:26257/swapzen_development?sslmode=disable

# -----------------------------------------------------------------------------
# REDIS (Optional but Recommended)
# -----------------------------------------------------------------------------
# For route caching - Falls back to memory store if not set
REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# RAILS (Required)
# -----------------------------------------------------------------------------
# Generate with: rails secret
SECRET_KEY_BASE=your_secret_key_base_here

# Environment: development, production, test
RAILS_ENV=development

# Puma thread count (affects database connection pool)
RAILS_MAX_THREADS=5

# Server port (default: 3000, suggest 3001 for pricing engine)
PORT=3001
